package org.example.roomnotificationtest_1.enums;

public enum ChatState {
    ROOM, // -> type
    MESSAGE, // -> photo | send | cancel
    PHOTO, NOTIFICATION_TYPE, IMAGE_OR_LAST, // -> send | cancel
}
